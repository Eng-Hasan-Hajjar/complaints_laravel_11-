


 <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">

        <meta name="description" content=" جامعة الشهباء ">
        <meta name="author" content="">

        <title> جامعة الشهباء </title>

        <!-- CSS FILES -->        
        <link href="{{ asset('website/css/bootstrap.min.css') }}" rel="stylesheet">
        <link href="{{ asset('website/css/bootstrap-icons.css') }}" rel="stylesheet">
        <link href="{{ asset('website/css/templatemo-kind-heart-charity.css') }}" rel="stylesheet">

        <!-- إضافة دعم RTL لـ Bootstrap إذا لم يكن موجوداً -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.rtl.min.css" rel="stylesheet">








        <style>

    .hero-university {
        background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
        color: white;
    }
    .hero-bg {
        position: absolute;
        top: 0; left: 0; right: 0; bottom: 0;
        z-index: 1;
    }
    .hero-bg-image {
        width: 100%;
        height: 100%;
        object-fit: cover;
        opacity: 0.3;
        filter: brightness(0.4) contrast(1.2);
    }
    .overlay {
        position: absolute;
        top: 0; left: 0; right: 0; bottom: 0;
        background: linear-gradient(to bottom, rgba(15,23,42,0.8), rgba(30,41,59,0.95));
        z-index: 2;
    }
    .z-10 { z-index: 10; }

    .hover-lift {
        transition: all 0.3s ease;
    }
    .hover-lift:hover {
        transform: translateY(-5px);
        box-shadow: 0 20px 40px rgba(0,0,0,0.3) !important;
    }

    .wave-bottom {
        position: absolute;
        bottom: 0;
        left: 0;
        width: 100%;
        z-index: 5;
    }
    .wave-bottom svg {
        width: 100%;
        height: 150px;
    }

    @media (max-width: 768px) {
        .display-4 { font-size: 2.5rem; }
        .lead { font-size: 1.1rem; }
    }

        </style>
    </head>



